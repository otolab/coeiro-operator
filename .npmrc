# pnpm設定

# 環境ごとのパッケージサポート
# macOS、Linux、Windowsの全環境でlockファイルを生成
supportedArchitectures.os=darwin,linux,win32
supportedArchitectures.cpu=x64,arm64

# オプショナル依存関係の扱い
# trueの場合、オプショナル依存関係のビルド失敗を許容
optional-dependencies=true

# ネイティブモジュールのビルド設定
# ビルドスクリプトを実行（speakerのネイティブビルドに必要）
ignore-scripts=false

# 特定パッケージのビルドを有効化
enable-pre-post-scripts=true